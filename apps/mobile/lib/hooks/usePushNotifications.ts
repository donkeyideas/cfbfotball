/**
 * Push notifications are disabled for initial App Store / Play Store release.
 * Re-enable after approval by restoring expo-notifications plugin in app.json
 * and uncommenting the registration logic below.
 */
export function usePushNotifications() {
  // no-op until app is approved on both stores
}
